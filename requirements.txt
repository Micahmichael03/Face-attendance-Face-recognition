opencv-python==4.6.0.66
Pillow==9.2.0
face_recognition==1.3.0
cmake==3.17.2
dlib==19.18.0


# if you are having trouble installing dlib, and the face_recognition package, you can try to install them separately
# i will advice to install them in the Anaconda environment. here is how i did it
# 1. open the Anaconda prompt
# 2. activate the environment you want to install the packages in
# 3. run the following commands
# conda install python=3.7 or 3.8
# conda install -c conda-forge cmake or just conda install cmake
# conda install -c conda-forge dlib
# pip install face_recognition
```